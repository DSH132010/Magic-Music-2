const tl = gsap.timeline() 

// tl.from('.line', { scaleY: 0, stagger: .2 })
// tl.from('.line-3', { scaleY: 0 })
//   .from('.line-4', { scaleY: 0 })
//   .from('.line-5', { scaleY: 0 })
//   .from('.line-6', { scaleY: 0 })
//   .from('.line-2', { scaleY: 0 })
//   .from('.line-1', { scaleY: 0 })
//   .from('.line-6', { scaleY: 0 })

// tl.from('.center .line', { scaleY: 0, })
//     .from('.left .line', { scaleY: 0, stagger: .2 })
